package ui.commands;

import ui.Command;

public class BuyCommand extends Command {
    public BuyCommand(String commandName) {
        super(commandName);
    }

    @Override
    public boolean execute() {
        return false;
    }
}
